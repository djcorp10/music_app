<h1>Listing songs</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Length</th>
    <th> artist</th>
    <th> album</th>
    <th> genre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @songs.each do |song| %>
  <tr>
    <td><%= song.name %></td>
    <td><%= song.length %></td>
    <td><%= song._artist_id %></td>
    <td><%= song._album_id %></td>
    <td><%= song._genre_id %></td>
    <td><%= link_to 'Show', song %></td>
    <td><%= link_to 'Edit', edit_song_path(song) %></td>
    <td><%= link_to 'Destroy', song, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Song', new_song_path %>
